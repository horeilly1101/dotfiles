#!/bin/zsh

git config --global user.name "Hugh O'Reilly III"
git config --global user.email "horeilly1101@gmail.com"
git config --global core.editor vim
git config --global core.excludesFile '~/.gitignore'
